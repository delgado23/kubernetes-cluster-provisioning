---
# =============================================================================
# Proxmox K8s VM Configuration Playbook
# =============================================================================
# Prerequisites:
#   ansible-galaxy collection install community.general
#
# What this playbook does (in order):
#   1. Discovers VM IDs from Proxmox by matching inventory hostnames
#   2. Shuts down each VM gracefully (ACPI) and waits for it to stop
#   3. Adds a 4M EFI disk with pre-enrolled secure boot keys on 'linstor'
#   4. Strips '.garaventaville.com' from the VM name
#   5. Applies tags: k8s+worker (workers) or k8s+controlplane (control planes)
#   6. Powers each VM back on and waits until the agent reports it running
#
# Inventory groups consumed (from Foreman via AWX):
#   foreman_almalinux10_kubernetescontrolplanenode
#   foreman_almalinux10_kubernetesworkernode
#
# Authentication:
#   Uses Proxmox API tokens (recommended for AWX).
#   Create a token in PVE: Datacenter → Permissions → API Tokens
#   Set proxmox_api_token in AWX as a custom credential or vault secret.
#   Token format expected: the token secret UUID only (not the full user!token=secret string).
#   Full header built as: "PVEAPIToken=<user>!<token_id>=<token_secret>"
#   If you prefer ticket (username+password) auth, see the commented
#   alternative at the bottom of this file.
# =============================================================================

- name: Configure Proxmox K8s VMs
  hosts: foreman_almalinux10_kubernetescontrolplanenode:foreman_almalinux10_kubernetesworkernode
  gather_facts: false
  serial: 1   # process one VM at a time to avoid hammering Proxmox API

  vars:
    # ------------------------------------------------------------------
    # Proxmox connection — override in AWX extra vars / vault
    # ------------------------------------------------------------------
    proxmox_host: "proxmox.example.com"
    proxmox_user: "root@pam"
    proxmox_token_id: "ansible"                        # the token ID (user@realm!token_id)
    proxmox_api_token: "{{ vault_proxmox_api_token }}" # the token secret UUID
    proxmox_password: "{{ vault_proxmox_password }}"   # only needed by proxmox_kvm module
    proxmox_node: "pve"
    proxmox_validate_certs: true

    # Convenience: full Authorization header value used by all uri tasks
    _pve_auth_header: "PVEAPIToken={{ proxmox_user }}!{{ proxmox_token_id }}={{ proxmox_api_token }}"

    # ------------------------------------------------------------------
    # EFI disk settings
    # ------------------------------------------------------------------
    efi_storage: "linstor"
    efi_type: "4m"

    # ------------------------------------------------------------------
    # Tag assignments
    # ------------------------------------------------------------------
    controlplane_tags: "k8s;controlplane"
    worker_tags: "k8s;worker"

    # ------------------------------------------------------------------
    # Timeouts (seconds)
    # ------------------------------------------------------------------
    shutdown_timeout: 120
    startup_timeout: 120

  tasks:

    # ------------------------------------------------------------------ #
    #  STEP 0 — Discover VMID via Proxmox API                            #
    # ------------------------------------------------------------------ #
    - name: Get full VM list from Proxmox node via API
      ansible.builtin.uri:
        url: "https://{{ proxmox_host }}/api2/json/nodes/{{ proxmox_node }}/qemu"
        method: GET
        headers:
          Authorization: "{{ _pve_auth_header }}"
        validate_certs: "{{ proxmox_validate_certs }}"
        return_content: true
      register: proxmox_vm_list
      delegate_to: localhost

    - name: Resolve short hostname (strip domain suffix)
      ansible.builtin.set_fact:
        short_hostname: "{{ inventory_hostname | regex_replace('\\..*$', '') }}"

    - name: Find VM entry matching this host
      ansible.builtin.set_fact:
        vm_info: >-
          {{
            proxmox_vm_list.json.data
            | selectattr('name', 'defined')
            | selectattr('name', 'search', short_hostname)
            | list
            | first
          }}

    - name: Fail clearly if VM was not found in Proxmox
      ansible.builtin.fail:
        msg: >
          Could not find a VM on node '{{ proxmox_node }}' whose name
          contains '{{ short_hostname }}'. Check that proxmox_node is correct
          and that the VM exists.
      when: vm_info is not defined or vm_info | length == 0

    - name: Set VMID and current name as facts
      ansible.builtin.set_fact:
        vmid: "{{ vm_info.vmid }}"
        vm_current_name: "{{ vm_info.name }}"

    - name: Display discovered VM details
      ansible.builtin.debug:
        msg: "Matched '{{ inventory_hostname }}' → VM '{{ vm_current_name }}' (VMID {{ vmid }})"

    # ------------------------------------------------------------------ #
    #  STEP 1 — Graceful shutdown                                         #
    # ------------------------------------------------------------------ #
    - name: Shut down VM gracefully (ACPI)
      community.general.proxmox_kvm:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: "{{ proxmox_validate_certs }}"
        node: "{{ proxmox_node }}"
        vmid: "{{ vmid }}"
        state: stopped
        force: false
        timeout: "{{ shutdown_timeout }}"
      delegate_to: localhost

    - name: Wait for VM to reach stopped state
      ansible.builtin.uri:
        url: "https://{{ proxmox_host }}/api2/json/nodes/{{ proxmox_node }}/qemu/{{ vmid }}/status/current"
        method: GET
        headers:
          Authorization: "{{ _pve_auth_header }}"
        validate_certs: "{{ proxmox_validate_certs }}"
        return_content: true
      register: vm_status
      until: vm_status.json.data.status == 'stopped'
      retries: "{{ (shutdown_timeout / 5) | int }}"
      delay: 5
      delegate_to: localhost

    # ------------------------------------------------------------------ #
    #  STEP 2 — Add EFI disk on linstor storage                          #
    # ------------------------------------------------------------------ #
    - name: Add EFI disk (4M, pre-enrolled secure boot keys) on linstor
      community.general.proxmox_kvm:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: "{{ proxmox_validate_certs }}"
        node: "{{ proxmox_node }}"
        vmid: "{{ vmid }}"
        efidisk0:
          storage: "{{ efi_storage }}"
          efitype: "{{ efi_type }}"
          pre_enrolled_keys: true
        update: true
      delegate_to: localhost

    # ------------------------------------------------------------------ #
    #  STEP 3 — Rename VM: remove .garaventaville.com                     #
    # ------------------------------------------------------------------ #
    - name: Compute cleaned VM name
      ansible.builtin.set_fact:
        vm_new_name: "{{ vm_current_name | regex_replace('\\.garaventaville\\.com$', '') }}"

    - name: Rename VM (strip .garaventaville.com)
      community.general.proxmox_kvm:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: "{{ proxmox_validate_certs }}"
        node: "{{ proxmox_node }}"
        vmid: "{{ vmid }}"
        name: "{{ vm_new_name }}"
        update: true
      delegate_to: localhost
      when: vm_new_name != vm_current_name

    - name: Skip rename notice (name already clean)
      ansible.builtin.debug:
        msg: "VM name '{{ vm_current_name }}' has no .garaventaville.com suffix — skipping rename."
      when: vm_new_name == vm_current_name

    # ------------------------------------------------------------------ #
    #  STEP 4 — Apply tags based on Foreman inventory group membership    #
    # ------------------------------------------------------------------ #
    - name: Resolve tags for control plane nodes
      ansible.builtin.set_fact:
        vm_tags: "{{ controlplane_tags }}"
      when: inventory_hostname in groups['foreman_almalinux10_kubernetescontrolplanenode']

    - name: Resolve tags for worker nodes
      ansible.builtin.set_fact:
        vm_tags: "{{ worker_tags }}"
      when: inventory_hostname in groups['foreman_almalinux10_kubernetesworkernode']

    - name: Apply tags to VM
      community.general.proxmox_kvm:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: "{{ proxmox_validate_certs }}"
        node: "{{ proxmox_node }}"
        vmid: "{{ vmid }}"
        tags: "{{ vm_tags }}"
        update: true
      delegate_to: localhost

    - name: Display applied tags
      ansible.builtin.debug:
        msg: "Applied tags '{{ vm_tags }}' to VM '{{ vm_new_name }}' (VMID {{ vmid }})"

    # ------------------------------------------------------------------ #
    #  STEP 5 — Power VM back on                                          #
    # ------------------------------------------------------------------ #
    - name: Start VM
      community.general.proxmox_kvm:
        api_host: "{{ proxmox_host }}"
        api_user: "{{ proxmox_user }}"
        api_password: "{{ proxmox_password }}"
        validate_certs: "{{ proxmox_validate_certs }}"
        node: "{{ proxmox_node }}"
        vmid: "{{ vmid }}"
        state: started
        timeout: "{{ startup_timeout }}"
      delegate_to: localhost

    - name: Wait for VM to reach running state
      ansible.builtin.uri:
        url: "https://{{ proxmox_host }}/api2/json/nodes/{{ proxmox_node }}/qemu/{{ vmid }}/status/current"
        method: GET
        headers:
          Authorization: "{{ _pve_auth_header }}"
        validate_certs: "{{ proxmox_validate_certs }}"
        return_content: true
      register: vm_started
      until: vm_started.json.data.status == 'running'
      retries: "{{ (startup_timeout / 5) | int }}"
      delay: 5
      delegate_to: localhost

    - name: Summary
      ansible.builtin.debug:
        msg: >
          ✓ VMID {{ vmid }} | Name: {{ vm_new_name }} | Tags: {{ vm_tags }}
          | EFI disk added on {{ efi_storage }} | Status: running